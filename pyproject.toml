[project]
name = "record-linkage-tutorial"
version = "0.1.0"
description = "A tutorial to understand record linkage"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.2",
    "pandas>=3.0.3",
    "splink>=4.0.16",
    "uv>=0.9",
]
